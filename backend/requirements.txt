# ChonkyFlipper Backend Dependencies
# Install with: pip3 install -r requirements.txt

flask==2.3.3
flask-cors==4.0.0
pigpio==1.78
spidev>=3.6
RPi.GPIO>=0.7.1
adafruit-circuitpython-pn532>=2.4.9
# pipower5  --  installed from /opt/pipower5/ by install.sh (not on PyPI)
# bleak==0.20.2  # For BLE - optional, requires Python 3.7+
# pyserial==3.5  # For UART modules if needed

# MQTT client for Zigbee2MQTT communication
paho-mqtt>=1.6.1

# Production WSGI server
gunicorn==21.2.0
